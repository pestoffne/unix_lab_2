#include <stdio.h>
#include <stdlib.h>

#include "process.h"

void process(char * logfile, char * command, char multiplex) {
    // DEBUG
    fprintf(stderr, "process(%s, %s, %i);\n", logfile, command, multiplex);

    
}
